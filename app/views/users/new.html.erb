<%= form_for @user, html: {multipart: true} do |f| %>

	<%= f.text_field :username, :placeholder => "Email Address" %>
	<%= f.password_field :password, :placeholder => "Password" %>
	<%= f.submit "Submit", :class=>"btn btn-primary"%>

<% end %>